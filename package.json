{
  "name": "@hi/bridge",
  "description": "boss hi 与 iframe 建立通讯实现",
  "private": true,
  "license": "ISC",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start": "pnpm -r --F=@hi/bridge-core run start",
    "release": "changeset publish",
    "publish:pre": "changeset pre enter alpha",
    "publish:pre:exit": "changeset pre exit"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/node": "^20.11.16",
    "typescript": "~4.5.2",
    "vite": "^2.6.4"
  }
}
